import React from 'react';
import PostsByTileContent from "../components/PostsByTitle";

const PostsByTitle = () => {
    return (
        <div>
            {/* A component which displays posts filter by title (search by title) content */}
            <PostsByTileContent />
        </div>
    );
};

export default PostsByTitle;