import React from 'react';
import HomeContent from '../components/Home';

const Home = () => {
    return (
        <div>
            {/* A component which displays the home content */}
            <HomeContent />
        </div>
    );
};

export default Home;
