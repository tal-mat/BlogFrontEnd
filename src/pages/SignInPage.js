import { SignInForm } from '../components/SignIn'; // Correct import statement

const SignInPage = () => {
    return (
        <div>
            <SignInForm /> {/* Use the correct component name here */}
        </div>
    );
};

export default SignInPage;
