import React from 'react';
import PostsByCategoryContent from '../components/PostsByCategory';

const PostsByCategory = () => {
    return (
        <div>
            {/* A component which displays the posts filters by category */}
            <PostsByCategoryContent />
        </div>
    );
};

export default PostsByCategory;