import React from 'react';
import ContactForm from '../components/Contact';

const ContactPage = () => {
    return (
        <div>
            {/* A component which display a form to send a message. */}
            <ContactForm />
        </div>
    );
};

export default ContactPage;